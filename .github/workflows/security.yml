name: security

on:
  pull_request:
    paths:
      - 'src/**'
      - 'test/**'
      - 'foundry.toml'
      - 'remappings.txt'
      - '.github/workflows/security.yml'
  push:
    branches: [main]
  schedule:
    # Nightly Mythril at 03:17 UTC; staggered off the hour to avoid GH peak contention.
    - cron: '17 3 * * *'

jobs:
  slither:
    name: slither static analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: nightly

      - name: Install dependencies
        run: |
          forge install --no-git foundry-rs/forge-std
          forge install --no-git OpenZeppelin/openzeppelin-contracts

      - name: Build
        run: forge build

      - name: Run Slither
        uses: crytic/slither-action@v0.4.0
        with:
          target: 'src/'
          fail-on: high
          slither-config: .github/slither.config.json
          ignore-compile: true

  mythril:
    name: mythril nightly scan
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Mythril
        run: pip install mythril

      - name: Run Mythril
        run: |
          for f in src/*.sol src/reference/*.sol; do
            myth analyze "$f" --solc-json mythril.solc.json -o markdown >> mythril-report.md || true
          done

      - name: Upload Mythril report
        uses: actions/upload-artifact@v4
        with:
          name: mythril-report
          path: mythril-report.md
